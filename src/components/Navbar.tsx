import {logoImg} from "../utils";

export function Navbar() {
    return (
        <>
        <header>
            <nav>
                <img src={logoImg}/>
            </nav>
        </header>
        </>
    );
}
